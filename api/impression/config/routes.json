{
  "routes": [
    {
      "method": "GET",
      "path": "/impressions",
      "handler": "impression.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/impressions/count",
      "handler": "impression.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/impressions/me",
      "handler": "impression.me",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/impressions/:id",
      "handler": "impression.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/impressions",
      "handler": "impression.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/impressions/:id",
      "handler": "impression.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/impressions/:id",
      "handler": "impression.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
